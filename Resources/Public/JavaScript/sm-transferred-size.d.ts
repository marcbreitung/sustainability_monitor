import type { Components, JSX } from "../../../Build/sustainability-monitor/dist/types/components";

interface SmTransferredSize extends Components.SmTransferredSize, HTMLElement {}
export const SmTransferredSize: {
    prototype: SmTransferredSize;
    new (): SmTransferredSize;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
